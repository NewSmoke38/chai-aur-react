import React from 'react';
import ReactDOM from 'react-dom/client';     // implimentation for web

import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));   // virtual dom made by react
root.render(
    <App />
);

